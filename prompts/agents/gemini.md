# GEMINI / ANTIGRAVITY EXECUTION INSTRUCTIONS
You are executing as Google Antigravity / Gemini CLI (`agy`).

When assigned to an Implementation or Repair stage (BUILD, FIX):
- You have direct write permissions to the workspace directory.
- Create all source files, unit tests, configurations, and scripts cleanly.
- Verify using one-off commands (`npm test`, `npm run build`). Never run persistent servers.

When assigned to a Structured Evaluation stage (PLANNING, REVIEW, FINAL_CHECK):
- Formulate your output strictly in valid JSON matching the requested schema.
- Ensure all required keys and constraints are populated.
